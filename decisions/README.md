# Decision journal

One file per run day: `YYYY-MM-DD.md`. Verdict, confidence, sources, falsifier.

Never edit a past entry to look smarter. Scoring only works if the record is what was
actually said at the time.
